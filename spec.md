# pr-codeowners 仕様書

## 概要

GitHub PRの変更ファイルをCODEOWNERSルールに基づいて分類し、各コードオーナー（チームまたは個人）ごとに担当ファイルとアサイン済みレビュアーを表示する。

## 入力

```
pr-codeowners [-v] <PR番号 or URL>
```

### 引数

| 引数 | 必須 | 説明 |
|------|------|------|
| PR番号 or URL | ○ | PR番号（例: `123`）またはPR URL（例: `https://github.com/org/repo/pull/123`） |

### オプション

| オプション | 説明 |
|------------|------|
| `-v` | 詳細ログを出力（タイムスタンプ付きの処理状況を表示） |

## 表示

- **通常モード**: スピナーアニメーションで処理中を表示し、完了後にJSONを出力
- **詳細モード（`-v`）**: タイムスタンプ付きのログを出力

## 出力

JSON形式で以下の構造を出力:

```json
{
  "<owner>": {
    "files": ["<file1>", "<file2>", ...],
    "reviewers": ["<user1>", "<user2>", ...]
  },
  ...
}
```

### フィールド説明

| フィールド | 型 | 説明 |
|-----------|------|------|
| `<owner>` | string | コードオーナー。`@org/team-name` 形式のチーム、または `@username` 形式の個人。オーナーがいないファイルは `(no owner)` にグループ化 |
| `files` | string[] | そのオーナーが担当する変更ファイルのパス一覧 |
| `reviewers` | string[] | そのオーナー（チーム）に所属し、かつPRにレビュアーとしてアサインされているユーザー。該当者がいない場合は省略 |

## レビュアーの判定ロジック

以下のいずれかに該当するユーザーをレビュアーとして扱う:

1. **reviewRequests**: PRにレビューをリクエストされているが、まだレビューしていないユーザー
2. **reviews (APPROVED)**: PRを `APPROVED` 状態でレビュー済みのユーザー

※ `COMMENTED` のみのユーザーや、`CHANGES_REQUESTED` のユーザーはレビュアーとして表示しない。

## チームメンバーの取得

コードオーナーが `@org/team-name` 形式のチームの場合、GitHub APIを使用してそのチームのメンバー一覧を取得し、レビュアーとのマッチングを行う。

## CODEOWNERSファイルの検索

以下の順序でCODEOWNERSファイルを検索（最初に見つかったものを使用）:

1. `.github/CODEOWNERS`
2. `CODEOWNERS`
3. `docs/CODEOWNERS`

## CODEOWNERSパターンマッチング

GitHubのCODEOWNERS仕様に準拠:

- 後に定義されたルールが優先される
- `*` は単一ディレクトリ内のファイルにマッチ
- `**` は任意の深さのパスにマッチ
- `/` で始まるパターンはリポジトリルートからの相対パス
- `/` で終わるパターンはディレクトリ配下すべてにマッチ

## 実行フロー

1. PR情報、変更ファイル一覧、CODEOWNERS、レビュアー情報を並列取得
2. 各チームオーナーのメンバー一覧を並列取得
3. 変更ファイルをCODEOWNERSルールでマッチング
4. 各オーナーに対して、所属レビュアーをフィルタリング
5. JSON形式で出力

## 依存関係

- GitHub CLI (`gh`): GitHub APIへのアクセスに使用
- `github.com/sourcegraph/conc`: 並列処理に使用
